const bodyBefore = document.querySelector('body');
const script = body.querySelector("header");
// insertBefore ( inserir antes de um elemento)

body.insertBefore(div, script);

// insertAfter (inserir depois de um elemento)
// body.insertAfter(div, script);